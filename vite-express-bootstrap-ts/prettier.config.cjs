const config = {
	trailingComma: 'es5',
	tabWidth: 4,
	useTabs: true,
	printWidth: 100,
	semi: true,
	singleQuote: true,
	quoteProps: 'consistent',
	arrowParens: 'avoid',
	// plugins: [require.resolve('prettier-plugin-ejs')],
};

module.exports = config;
